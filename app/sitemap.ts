import type {MetadataRoute} from "next"; export const dynamic="force-dynamic"; import {prisma} from "@/lib/db"; import {absolute} from "@/lib/utils"; export default async function sitemap():Promise<MetadataRoute.Sitemap>{const[p,a,u,j,n]=await Promise.all([prisma.person.findMany({where:{status:"PUBLISHED"},select:{slug:true,updatedAt:true}}),prisma.researchArea.findMany({where:{status:"PUBLISHED"},select:{slug:true,updatedAt:true}}),prisma.publication.findMany({where:{status:"PUBLISHED"},select:{slug:true,updatedAt:true}}),prisma.project.findMany({where:{publishStatus:"PUBLISHED"},select:{slug:true,updatedAt:true}}),prisma.newsPost.findMany({where:{status:"PUBLISHED"},select:{slug:true,updatedAt:true}})]);const base=["/","/research","/people","/publications","/projects","/news-events","/join","/about","/contact"].map(x=>({url:absolute(x),lastModified:new Date()}));return[...base,...p.map(x=>({url:absolute(`/people/${x.slug}`),lastModified:x.updatedAt})),...a.map(x=>({url:absolute(`/research/${x.slug}`),lastModified:x.updatedAt})),...u.map(x=>({url:absolute(`/publications/${x.slug}`),lastModified:x.updatedAt})),...j.map(x=>({url:absolute(`/projects/${x.slug}`),lastModified:x.updatedAt})),...n.map(x=>({url:absolute(`/news/${x.slug}`),lastModified:x.updatedAt}))]}
