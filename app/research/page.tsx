import {prisma} from "@/lib/db"; import {AreaCard,SectionHeader} from "@/components/ui"; export default async function Page(){const a=await prisma.researchArea.findMany({where:{status:"PUBLISHED"},orderBy:{sortOrder:"asc"}});return <div className="container-site section-space"><SectionHeader eyebrow="Research" title="Research areas" description="Each area connects questions to people, active projects and scholarship."/><div>{a.map((x,i)=><AreaCard key={x.id} a={x} i={i}/>)}</div></div>}
