"use client"; import {FormEvent,useState} from "react"; import {signIn} from "next-auth/react"; import {useRouter} from "next/navigation";
export default function Page(){const r=useRouter(),[err,setErr]=useState(""),[busy,setBusy]=useState(false);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);const f=new FormData(e.currentTarget),x=await signIn("credentials",{email:f.get("email"),password:f.get("password"),redirect:false,callbackUrl:"/admin"});setBusy(false);if(x?.error)setErr("Invalid email or password.");else{r.push("/admin");r.refresh()}}return <div className="container-site flex min-h-[70vh] items-center justify-center py-16"><div className="surface w-full max-w-md border academic-rule p-7"><div className="text-xs font-bold uppercase tracking-[.18em] text-accent">Administration</div><h1 className="mt-2 font-serif text-4xl">Sign in</h1>{err&&<div className="mt-5 border border-red-700/30 p-3 text-sm">{err}</div>}<form onSubmit={submit} className="mt-7 grid gap-5"><label className="grid gap-2 text-sm font-semibold">Email<input required type="email" name="email" autoComplete="username" className="surface border academic-rule px-3 py-3 font-normal"/></label><label className="grid gap-2 text-sm font-semibold">Password<input required type="password" name="password" autoComplete="current-password" className="surface border academic-rule px-3 py-3 font-normal"/></label><button disabled={busy} className="bg-[#172a46] px-5 py-3 text-white">{busy?"Signing in…":"Sign in"}</button></form></div></div>}
