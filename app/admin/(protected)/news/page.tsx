import Link from "next/link"; import {prisma} from "@/lib/db"; import {requireEditor} from "@/lib/admin"; import {CONTENT_STATUSES} from "@/lib/constants"; import {fmt} from "@/lib/utils"; import {saveNews,deleteEntity} from "@/app/admin/actions"; import {Field,Heading,Status,inputClass,textareaClass} from "@/components/admin/common"; import {DeleteButton} from "@/components/admin/delete-button";
export default async function Page({searchParams}:{searchParams:Promise<{edit?:string;saved?:string;error?:string}>}){const[q,s,items]=await Promise.all([searchParams,requireEditor(),prisma.newsPost.findMany({orderBy:{date:"desc"}})]);const c=q.edit?await prisma.newsPost.findUnique({where:{id:q.edit}}):null;return <><Heading title="News" editing={!!c} description="Publication announcements, member updates, visitors and group activity."/><Status saved={q.saved} error={q.error}/><form action={saveNews} className="grid gap-5 border academic-rule p-5"><input type="hidden" name="id" value={c?.id||""}/><div className="grid gap-4 md:grid-cols-2"><Field label="Title"><input required name="title" defaultValue={c?.title||""} className={inputClass}/></Field><Field label="Slug"><input name="slug" defaultValue={c?.slug||""} className={inputClass}/></Field><Field label="Date"><input required type="date" name="date" defaultValue={(c?.date||new Date()).toISOString().slice(0,10)} className={inputClass}/></Field><Field label="Status"><select name="status" defaultValue={c?.status||"PUBLISHED"} className={inputClass}>{CONTENT_STATUSES.map(x=><option key={x}>{x}</option>)}</select></Field><Field label="Image path"><input name="image" defaultValue={c?.image||""} className={inputClass}/></Field><Field label="External URL"><input name="externalUrl" defaultValue={c?.externalUrl||""} className={inputClass}/></Field><Field label="Display order"><input type="number" name="sortOrder" defaultValue={c?.sortOrder??0} className={inputClass}/></Field><label className="flex items-center gap-2 self-end pb-3 text-sm"><input type="checkbox" name="featured" defaultChecked={c?.featured||false}/> Featured news</label></div><Field label="Summary"><textarea required name="summary" rows={3} defaultValue={c?.summary||""} className={textareaClass}/></Field><Field label="Body"><textarea required name="body" rows={8} defaultValue={c?.body||""} className={textareaClass}/></Field><button className="justify-self-start bg-[#172a46] px-5 py-3 text-white">{c?"Save news":"Create news"}</button></form><section className="mt-10">{items.map(x=><div key={x.id} className="grid items-center gap-3 border-b academic-rule py-4 md:grid-cols-[120px_1fr_120px_auto]"><div className="text-xs text-muted">{fmt(x.date)}</div><div><strong>{x.title}</strong><div className="text-xs text-muted">{x.status}{x.featured?" · Featured":""}</div></div><Link className="link-academic text-xs" href={`/news/${x.slug}`} target="_blank">Public ↗</Link><div className="flex gap-2"><Link className="border academic-rule px-3 py-2 text-xs" href={`?edit=${x.id}`}>Edit</Link>{s.user.role==="ADMIN"&&<form action={deleteEntity}><input type="hidden" name="model" value="news"/><input type="hidden" name="id" value={x.id}/><DeleteButton/></form>}</div></div>)}</section></>}
