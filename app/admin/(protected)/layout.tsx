import type {ReactNode} from "react"; import {requireEditor} from "@/lib/admin"; import {Sidebar} from "@/components/admin/sidebar"; import {Logout} from "@/components/admin/logout"; export default async function Layout({children}:{children:ReactNode}){const s=await requireEditor();return <div className="container-site py-8"><div className="mb-5 flex justify-between border-b academic-rule pb-5"><div className="text-sm">Signed in as <strong>{s.user.name||s.user.email}</strong> · {s.user.role}</div><Logout/></div><div className="grid border academic-rule lg:grid-cols-[230px_1fr]"><Sidebar/><div className="min-w-0 p-5 md:p-8">{children}</div></div></div>}
