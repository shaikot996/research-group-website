import Link from "next/link"; import {prisma} from "@/lib/db"; import {requireEditor} from "@/lib/admin"; import {CONTENT_STATUSES} from "@/lib/constants"; import {savePage,deleteEntity} from "@/app/admin/actions"; import {Field,Heading,Status,inputClass,textareaClass} from "@/components/admin/common"; import {DeleteButton} from "@/components/admin/delete-button";
export default async function Page({searchParams}:{searchParams:Promise<{edit?:string;saved?:string;error?:string}>}){const[q,s,items]=await Promise.all([searchParams,requireEditor(),prisma.page.findMany({orderBy:[{sortOrder:"asc"},{title:"asc"}]})]);const c=q.edit?await prisma.page.findUnique({where:{id:q.edit}}):null;return <><Heading title="Pages" editing={!!c} description="Editable long-form content including About and Join Us."/><Status saved={q.saved} error={q.error}/><form action={savePage} className="grid gap-5 border academic-rule p-5"><input type="hidden" name="id" value={c?.id||""}/><div className="grid gap-4 md:grid-cols-2"><Field label="Title"><input required name="title" defaultValue={c?.title||""} className={inputClass}/></Field><Field label="Slug"><input name="slug" defaultValue={c?.slug||""} className={inputClass}/></Field><Field label="Eyebrow"><input name="eyebrow" defaultValue={c?.eyebrow||""} className={inputClass}/></Field><Field label="Status"><select name="status" defaultValue={c?.status||"PUBLISHED"} className={inputClass}>{CONTENT_STATUSES.map(x=><option key={x}>{x}</option>)}</select></Field><Field label="Display order"><input type="number" name="sortOrder" defaultValue={c?.sortOrder??0} className={inputClass}/></Field></div><Field label="Body" help="Blank lines create paragraphs; ## and ### create headings."><textarea required name="body" rows={14} defaultValue={c?.body||""} className={textareaClass}/></Field><button className="justify-self-start bg-[#172a46] px-5 py-3 text-white">{c?"Save page":"Create page"}</button></form><section className="mt-10">{items.map(x=>{const href=x.slug==="about"||x.slug==="join"?`/${x.slug}`:`/pages/${x.slug}`;return <div key={x.id} className="grid items-center gap-3 border-b academic-rule py-4 md:grid-cols-[1fr_120px_auto]"><div><strong>{x.title}</strong><div className="text-xs text-muted">/{x.slug} · {x.status}</div></div><Link className="link-academic text-xs" href={href} target="_blank">Public ↗</Link><div className="flex gap-2"><Link className="border academic-rule px-3 py-2 text-xs" href={`?edit=${x.id}`}>Edit</Link>{s.user.role==="ADMIN"&&<form action={deleteEntity}><input type="hidden" name="model" value="page"/><input type="hidden" name="id" value={x.id}/><DeleteButton/></form>}</div></div>})}</section></>}
