import {prisma} from "@/lib/db"; import {RichText,SectionHeader} from "@/components/ui"; export default async function Page(){const p=await prisma.page.findFirst({where:{slug:"join",status:"PUBLISHED"}});return <div className="container-site section-space max-w-5xl"><SectionHeader eyebrow={p?.eyebrow||"Opportunities"} title={p?.title||"Join Us"} description="Vacancies and supervision capacity depend on funding and current availability."/>{p&&<RichText text={p.body}/>}</div>}
