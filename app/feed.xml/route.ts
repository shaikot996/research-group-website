import {prisma} from "@/lib/db"; import {getSiteSettings} from "@/lib/site"; import {absolute} from "@/lib/utils"; const xml=(s:string)=>s.replace(/&/g,"&amp;").replace(/</g,"&lt;").replace(/>/g,"&gt;").replace(/\"/g,"&quot;"); export async function GET(){const s=await getSiteSettings();const n=await prisma.newsPost.findMany({where:{status:"PUBLISHED"},orderBy:{date:"desc"},take:20});const items=n.map(x=>`<item><title>${xml(x.title)}</title><link>${absolute(`/news/${x.slug}`)}</link><guid>${absolute(`/news/${x.slug}`)}</guid><pubDate>${x.date.toUTCString()}</pubDate><description>${xml(x.summary)}</description></item>`).join("");return new Response(`<?xml version="1.0"?><rss version="2.0"><channel><title>${xml(s.groupName)} News</title><link>${absolute("/")}</link><description>Research group news</description>${items}</channel></rss>`,{headers:{"Content-Type":"application/rss+xml; charset=utf-8"}})}
