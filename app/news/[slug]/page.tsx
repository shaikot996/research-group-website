import {notFound} from "next/navigation"; import {prisma} from "@/lib/db"; import {Breadcrumbs,RichText} from "@/components/ui"; import {fmt} from "@/lib/utils"; export default async function Page({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const n=await prisma.newsPost.findFirst({where:{slug,status:"PUBLISHED"}});if(!n)notFound();return <article className="container-site section-space max-w-4xl"><Breadcrumbs items={[{label:"News & Events",href:"/news-events"},{label:n.title}]}/><div className="text-xs font-bold uppercase tracking-[.18em] text-accent">News · {fmt(n.date)}</div><h1 className="mt-4 section-heading">{n.title}</h1><p className="mt-6 text-xl leading-8 text-muted">{n.summary}</p><div className="mt-10"><RichText text={n.body}/></div></article>}
