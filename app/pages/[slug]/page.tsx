import {notFound} from "next/navigation"; import {prisma} from "@/lib/db"; import {Breadcrumbs,RichText} from "@/components/ui"; export default async function Page({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const p=await prisma.page.findFirst({where:{slug,status:"PUBLISHED"}});if(!p)notFound();return <div className="container-site section-space max-w-5xl"><Breadcrumbs items={[{label:p.title}]}/><div className="text-xs font-bold uppercase tracking-[.18em] text-accent">{p.eyebrow}</div><h1 className="mt-3 section-heading">{p.title}</h1><div className="mt-10"><RichText text={p.body}/></div></div>}
