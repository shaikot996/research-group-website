import {prisma} from "@/lib/db"; const stamp=(d:Date)=>d.toISOString().replace(/[-:]/g,"").replace(/\.\d{3}Z$/,"Z"); const esc=(s="")=>s.replace(/\\/g,"\\\\").replace(/\n/g,"\\n").replace(/,/g,"\\,").replace(/;/g,"\\;"); export async function GET(_:Request,{params}:{params:Promise<{slug:string}>}){const{slug}=await params;const e=await prisma.event.findUnique({where:{slug}});if(!e||e.publishStatus!=="PUBLISHED")return new Response("Not found",{status:404});const end=e.endDate??new Date(e.date.getTime()+3600000);const body=["BEGIN:VCALENDAR","VERSION:2.0","PRODID:-//FPCG//Events//EN","BEGIN:VEVENT",`UID:${e.id}@fpcg`,`DTSTAMP:${stamp(new Date())}`,`DTSTART:${stamp(e.date)}`,`DTEND:${stamp(end)}`,`SUMMARY:${esc(e.title)}`,`LOCATION:${esc(e.location||"")}`,`DESCRIPTION:${esc(e.abstract||"")}`,"END:VEVENT","END:VCALENDAR"].join("\r\n");return new Response(body,{headers:{"Content-Type":"text/calendar; charset=utf-8","Content-Disposition":`attachment; filename="${e.slug}.ics"`}})}
