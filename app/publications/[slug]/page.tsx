import Link from "next/link"; import {notFound} from "next/navigation"; import {prisma} from "@/lib/db"; import {Breadcrumbs} from "@/components/ui"; import {CopyButton} from "@/components/copy"; import {absolute,humanize} from "@/lib/utils";
export default async function Page({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const p=await prisma.publication.findFirst({where:{slug,status:"PUBLISHED"},include:{authors:true,projects:true,researchAreas:true}});if(!p)notFound();const ld={"@context":"https://schema.org","@type":"ScholarlyArticle",headline:p.title,author:p.authors.map(a=>({"@type":"Person",name:a.name,url:absolute(`/people/${a.slug}`)})),datePublished:String(p.year),description:p.abstract,url:absolute(`/publications/${p.slug}`)};return <div className="container-site section-space max-w-5xl"><script type="application/ld+json" dangerouslySetInnerHTML={{__html:JSON.stringify(ld)}}/><Breadcrumbs items={[{label:"Publications",href:"/publications"},{label:p.title}]}/><div className="text-xs font-bold uppercase tracking-[.18em] text-accent">{humanize(p.type)} · {p.year}</div><h1 className="mt-4 font-serif text-5xl leading-tight">{p.title}</h1><p className="mt-6 text-lg text-muted">{p.authorText}</p><div className="mt-8 flex gap-4 text-sm">{p.doi&&<a className="link-academic" href={`https://doi.org/${p.doi}`}>DOI</a>}{p.arxivId&&<a className="link-academic" href={`https://arxiv.org/abs/${p.arxivId}`}>arXiv</a>}{p.inspireUrl&&<a className="link-academic" href={p.inspireUrl}>INSPIRE-HEP</a>}{p.pdfUrl&&<a className="link-academic" href={p.pdfUrl}>PDF</a>}</div>{p.abstract&&<section className="mt-12"><h2 className="font-serif text-3xl">Abstract</h2><p className="mt-4 leading-8 text-muted">{p.abstract}</p></section>}{p.authors.length>0&&<section className="mt-10"><h2 className="font-serif text-2xl">Group authors</h2><div className="mt-3 flex flex-wrap gap-2">{p.authors.map(a=><Link className="border academic-rule px-3 py-2 text-sm" key={a.id} href={`/people/${a.slug}`}>{a.name}</Link>)}</div></section>}{p.bibtex&&<section className="mt-12 border-t academic-rule pt-7"><div className="flex justify-between"><h2 className="font-serif text-2xl">BibTeX</h2><CopyButton value={p.bibtex}/></div><pre className="mt-4 overflow-x-auto border academic-rule p-4 text-xs"><code>{p.bibtex}</code></pre></section>}</div>}
